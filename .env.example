# Piri v3 — Ortam Değişkenleri
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun.
#
# cp .env.example .env

# ─── OpenAI Backend (opsiyonel ama ŞİDDETLE önerilen) ──────
# API key eklerseniz Piri, gpt-4o-mini ile çok daha kaliteli cevaplar üretir.
# Key almak için: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# OpenAI modeli (varsayılan: gpt-4o-mini, alternatif: gpt-4o, gpt-3.5-turbo)
# OPENAI_MODEL=gpt-4o-mini

# ─── Local Model Ayarları ─────────────────────────────────────
# Fine-tuned model yolu (varsayılan: Qwen2.5-0.5B-Instruct)
# PIRI_MODEL_PATH=./model

# ─── RAG Ayarları ─────────────────────────────────────────────
# Embedding modeli (varsayılan: intfloat/multilingual-e5-small)
# PIRI_EMBEDDING_MODEL=intfloat/multilingual-e5-small

# Reranking aktif mi (varsayılan: true)
# PIRI_RERANKING=true
